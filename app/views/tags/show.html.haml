%h1 Tags#show
%p Find me in app/views/tags/show.html.haml

= @tag.name

%p
  %b parent:
  = @tag.parent ? @tag.parent.name : 'none'

%p
  %b children
  - @tag.children.each do |child|
    = link_to child.name, child

%br
= link_to 'Back', tags_path
