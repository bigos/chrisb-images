%h3  Photos


  
%table
  %tr
    %th Attachment file name
    %th Tags
    %th
    %th
    %th

  - @photos.each do |photo|
    %tr
      %td
        = link_to image_tag(photo.attachment.url(:thumb)), photo.attachment.url, {:class => 'fancybox', :title => photo.attachment_file_name[0..-5].gsub('_',' ')}
           

      %td
        = link_to 'contact', '#inline-test', {:class => 'fancydiv'}
        = photo.tags.count
      %td= link_to 'Show', photo
      %td= link_to 'Edit', edit_photo_path(photo)
      %td= link_to 'Destroy', photo, :method => :delete, :data => { :confirm => 'Are you sure?' }
= will_paginate @photos
%br

= link_to 'New Photo', new_photo_path

= link_to 'Bulk Photo Upload', '/bulk_upload/new'

%div{:style=>''}
  #inline-test
    %p test content